---
interface DataPoint {
  label: string;
  value: number;
}

interface Props {
  data: DataPoint[];
  title?: string;
}

const { data, title } = Astro.props;
const max = Math.max(...data.map((d) => d.value), 1);
const barHeight = 160;
---

{data.length > 0 && (
  <figure class="my-8 rounded-xl border border-[var(--color-border)] bg-[var(--color-bg-elevated)] p-6">
    {title && <figcaption class="mb-4 font-sans text-sm font-semibold text-[var(--color-text)]">{title}</figcaption>}
    <svg
      viewBox={`0 0 ${data.length * 80} ${barHeight + 40}`}
      class="w-full max-w-lg text-cyan-400"
      role="img"
      aria-label={title ?? 'Pylväskaavio'}
    >
      {data.map((point, i) => {
        const h = (point.value / max) * barHeight;
        const x = i * 80 + 20;
        const y = barHeight - h + 10;
        return (
          <g>
            <rect
              x={x}
              y={y}
              width="40"
              height={h}
              rx="4"
              fill="currentColor"
              opacity="0.85"
            />
            <text
              x={x + 20}
              y={barHeight + 28}
              text-anchor="middle"
              class="fill-[var(--color-text-muted)] text-[10px]"
              font-family="system-ui, sans-serif"
            >
              {point.label}
            </text>
            <text
              x={x + 20}
              y={y - 6}
              text-anchor="middle"
              class="fill-[var(--color-text-dim)] text-[10px]"
              font-family="system-ui, sans-serif"
            >
              {point.value}
            </text>
          </g>
        );
      })}
    </svg>
  </figure>
)}
