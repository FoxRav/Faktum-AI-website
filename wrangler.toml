name = "faktum-ai-website"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-01"

# KV binding FAKTUM_VISITS is configured in Cloudflare Pages dashboard:
# Settings → Bindings → KV namespace (Production).
